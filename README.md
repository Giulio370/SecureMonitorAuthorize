This program takes as input the Json file created by the "MonitorManifest" program and creates a txt authorization file encrypted via key (32 characters)
Called "PlayerCertificate_Your_Monitor_ID__DateTime"
